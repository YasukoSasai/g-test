# バックエンドアプリの起動方法

### 1. DBを起動する
```console
$ コンテナ起動
$ cd postgres
$ sh ./start.sh
```

### 2. Spring-Bootを起動する
```console
srcのbuild.gradleをリフレッシュ
STSでMyFirstApplicationプロジェクトを起動し、Spring Boot Appで起動
```
